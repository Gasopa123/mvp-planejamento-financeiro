# Logged QA — 2026-09-03

Target: production deployment `eb9ec31`.

## Result

Status: partial. Full logged browser QA is blocked because the QA signup flow returns no active session and the test account cannot log in before email confirmation.

## Verified

Technical gates on `main`:

```text
npm test -- --run: 185 passed
./node_modules/.bin/eslint . --format stylish: OK
npm run build: OK
npx tsc --noEmit: OK
git diff --check: OK
```

Production HTTP smoke:

```text
/             200, login page rendered
/login        200, login page rendered
/cadastro     200, signup page rendered
/carteira     200, unauthenticated request does not leak client data
/carteira/novo 200, unauthenticated request does not leak client data
```

Supabase/Auth smoke:

```text
Public Supabase config exists in the production bundle.
Signup request with a QA email returns 200.
No session is returned after signup.
Password login immediately after signup returns 400.
```

## Not verified

These still need a confirmed/logged-in advisor account:

- create client through the UI
- save full wizard data
- reload and confirm persistence
- edit client and confirm recalculation
- verify dashboard numbers in browser
- verify presentation mode in browser
- verify RLS with two confirmed advisor accounts

## Next smallest step

Use a dedicated QA advisor account with confirmed email, then rerun the browser flow end-to-end.

Do not use Gabriel's personal password for this QA.
